---
title: "Indice de Precios de Alimentos"
description: |
  Este es un proyecto en el que me propuse medir la evolución de precios de alimentos en páginas web de los principales supermercados del país. Todo el proceso desde el web scraping para obtener los datos, el cálculo y la publicación las realizo en el lenguaje de programación R. Para obtener las ponderaciones utilicé la Encuesta Nacional de Gastos de los Hogares (ENGHo) 2017-2018. Los resultados presentados no contienen ninguna información sobre el negocio informante, manteniendo esta información en reserva.
site: distill::distill_website
---


# Resultados acumulados a la cuarta semana de Abril de 2021

El índice de marzo de 2021 esta compuesto por los precios de la última semana del mes ya que en ese momento fue cuando inicie la recolección de datos, debido a esto las variaciones del corriente mes pueden estar subestimadas.

```{r, echo=FALSE}
htmltools::includeHTML("../../06.Graficos_tablas/cuadro1.html")
```

# Evolución semanal

```{r, echo=FALSE}
library(dplyr)
library(highcharter)

indices_mensuales <- readRDS("../../01.Bases/02.Clean/02.Calculo/indices_mensuales_por_semana.rds")
canasta_alim <- readRDS("../../01.Bases/02.Clean/03.Canasta/canasta_alim.rds")

indices_mensuales <- indices_mensuales %>% arrange(coicop) %>% mutate(coicop = factor(coicop, levels = unique(coicop)))


# Gráficos ----------------------------------------------------------------


db_grafico <- indices_mensuales %>% left_join(canasta_alim %>% select(coicop, descripcion, nivel), by = "coicop") %>% 
  mutate(periodo = if_else(is.na(periodo_max), "4° semana de marzo 2021", paste(periodo_max)))
grafico <- hchart(db_grafico %>% filter(nivel==4), "line", hcaes(x = periodo, y = round(indice_mes, 2), group = descripcion)) %>%
  hc_title(text = "Evolución de indices de Alimentos") %>% 
  hc_subtitle(text = "Aperturas a nivel subclase") %>% 
  hc_xAxis(title = list(text = "Período")) %>% 
  hc_yAxis(title = list(text = "Indice")) %>% 
  hc_size(1000, 800) 

grafico

